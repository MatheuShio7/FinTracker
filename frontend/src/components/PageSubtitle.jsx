import './PageSubtitle.css'

function PageSubtitle({ subtitle }) {
  return (
    <div className="page-subtitle">
      <h2 className="page-subtitle-text">{subtitle}</h2>
    </div>
  )
}

export default PageSubtitle

